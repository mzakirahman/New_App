import 'package:flutter/material.dart';

class HomeViuw extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("HomeViuw"),
      )
    );
  }
}